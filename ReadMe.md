# Pre-commit Configs

This repository stores [pre-commit](https://pre-commit.com/index.html) config files.

## Types of hooks

The hooks used in the config files are configured not to alter the codebase drastically. Hooks only perform checks and format the syntax.

> [!important]
> Hooks that rewrite the code are not allowed.
